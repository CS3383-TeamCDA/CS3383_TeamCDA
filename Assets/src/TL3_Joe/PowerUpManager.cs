using System.Numerics;

public class PowerUpManager 
{

    public enum PowerUpType { SpeedBoost = 0, Shield = 1, Magnet = 2 }
    public void SpawnPowerUp(Vector3 pos, PowerUpType type)
    {
        // Code here
    }
}